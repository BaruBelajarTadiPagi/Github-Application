<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adityabrian.consumerapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.adityabrian.githubapp.READ_DATABASE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubApp">
        <activity android:name=".UI.Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <!-- sebenernya queries ini tak terpakai, karena kita tidak menggunakan query di url nya tadi
    BARANG KALI INGIN DIPAKAI, INI SANGAT BERGUNA -->
    <queries>
        <package android:name="com.adityabrian.githubapp"/>
    </queries>

</manifest>